git config --global user.name "tsmithcode"
git config --global user.email "thomas@tsmithcode.ai"

git remote add origin https://github.com/tsmithcode/recruiter-showcase.git
git branch -M main
git push -u origin main
